# OpenAI API Configuration
# Required: Your OpenAI API key for GPT-4 Vision
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Maximum file size for uploads (in bytes, default: 10MB)
# MAX_FILE_SIZE=10485760

# Optional: Allowed image formats (comma-separated)
# ALLOWED_IMAGE_FORMATS=image/jpeg,image/png,image/webp,image/heic

# Optional: Allowed dev origins for Next.js (comma-separated)
# Used for development with tunnels, codespaces, etc.
# ALLOWED_DEV_ORIGINS=127.0.0.1,localhost,your-tunnel.example.com

# Optional: Allowed origin for CORS in production
# Use '*' for any origin, or a specific domain like 'https://myapp.com'
# Leave empty to disable CORS headers
# ALLOWED_PROD_ORIGINS=*

# ============================================================================
# Rate Limiting Configuration
# ============================================================================

# Maximum number of workout analyses per user per day (default: 5)
# DAILY_PARSE_LIMIT=5

# Enable/disable rate limiting (default: true)
# Set to 'false' to disable rate limiting entirely
# RATE_LIMIT_ENABLED=true

# Redis URL for distributed rate limiting (optional)
# If not provided, falls back to in-memory storage (single-instance only)
# Supports: redis://, rediss:// (TLS), and Upstash REST URLs
# Examples:
#   Local: redis://localhost:6379
#   With password: redis://:password@localhost:6379
#   Upstash: rediss://default:xxx@xxx.upstash.io:6379
#   Railway/Render: Use the provided REDIS_URL
# REDIS_URL=redis://localhost:6379
